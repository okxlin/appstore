services:
  isrvd:
    image: "rehiy/isrvd:slim@sha256:a2489d861047f4b266c37f93f664fe2c0cecd7be4853dd95f4b5580a3a7100fd"
    container_name: ${CONTAINER_NAME}
    entrypoint:
      - /data/conf/app-entrypoint.sh
    ports:
      - "${PANEL_APP_PORT_HTTP}:8080"
    volumes:
      - "${ISRVD_DATA_ABS}:/data"
      - "${ISRVD_DATA_ABS}:${ISRVD_DATA_ABS}"
      - "/var/run/docker.sock:/var/run/docker.sock"
    healthcheck:
      test:
        - CMD
        - wget
        - --spider
        - -q
        - http://127.0.0.1:8080/
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s
    labels:
      createdBy: Apps
    networks:
      - 1panel-network
    restart: unless-stopped

networks:
  1panel-network:
    external: true
