services:
  nxv:
    image: "ghcr.io/utensils/nxv:0.7.1@sha256:892ac4da12bcc350f980dc7ce743cbda2a9ed5bcdccae587290f284c87b00215"
    container_name: ${CONTAINER_NAME}
    command:
      - --db-path
      - /data/index.db
      - serve
      - --host
      - 0.0.0.0
      - --port
      - "8080"
    ports:
      - "${PANEL_APP_PORT_HTTP}:8080"
    volumes:
      - "${APP_DATA_DIR}:/data"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - nxv
        - --db-path
        - /data/index.db
        - stats
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      createdBy: "Apps"
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
