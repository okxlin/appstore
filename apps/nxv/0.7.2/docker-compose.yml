services:
  nxv:
    image: "ghcr.io/utensils/nxv:0.7.2@sha256:28e3f559195e47ba2775ed1bea62a767f42216edb8b8059e4de8574dd687496b"
    container_name: ${CONTAINER_NAME}
    command:
      - --db-path
      - /data/index.db
      - serve
      - --host
      - 0.0.0.0
      - --port
      - "8080"
    ports:
      - "${PANEL_APP_PORT_HTTP}:8080"
    volumes:
      - "${APP_DATA_DIR}:/data"
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - nxv
        - --db-path
        - /data/index.db
        - stats
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    labels:
      createdBy: "Apps"
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
