additionalProperties:
  formFields:
    - default: 6800
      edit: true
      envKey: RPC_PORT_VALUE
      labelEn: RPC port
      labelZh: RPC端口
      label:
        en: RPC port
        zh: RPC端口
        zh-Hant: RPC端口
        ja: RPC port
        ko: RPC port
        ru: RPC port
        ms: RPC port
        pt-br: RPC port
      required: true
      rule: paramPort
      type: number
    - default: 6888
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Listening port
      labelZh: 监听端口
      label:
        en: Listening port
        zh: 监听端口
        zh-Hant: 监听端口
        ja: Listening port
        ko: Listening port
        ru: Listening port
        ms: Listening port
        pt-br: Listening port
      required: true
      rule: paramPort
      type: number
    - default: aria2
      envKey: TOKEN
      labelEn: RPC secret
      labelZh: RPC密钥
      label:
        en: RPC secret
        zh: RPC密钥
        zh-Hant: RPC密钥
        ja: RPC secret
        ko: RPC secret
        ru: RPC secret
        ms: RPC secret
        pt-br: RPC secret
      random: true
      required: true
      rule: paramCommon
      type: text
    - default: ./data/config
      edit: true
      envKey: CONFIG_PATH
      labelEn: Path to the configuration file
      labelZh: 配置文件所在路径
      label:
        en: Path to the configuration file
        zh: 配置文件所在路径
        zh-Hant: 配置文件所在路径
        ja: Path to the configuration file
        ko: Path to the configuration file
        ru: Path to the configuration file
        ms: Path to the configuration file
        pt-br: Path to the configuration file
      required: true
      type: text
    - default: ./data/downloads
      edit: true
      envKey: DOWNLOAD_PATH
      labelEn: Download folder path
      labelZh: 下载文件夹路径
      label:
        en: Download folder path
        zh: 下载文件夹路径
        zh-Hant: 下载文件夹路径
        ja: Download folder path
        ko: Download folder path
        ru: Download folder path
        ms: Download folder path
        pt-br: Download folder path
      required: true
      type: text
    - default: https://trackerslist.com/all_aria2.txt
      edit: true
      envKey: TRACKER_URL
      labelEn: TRACKER URL
      labelZh: TRACKER链接
      label:
        en: TRACKER URL
        zh: TRACKER链接
        zh-Hant: TRACKER链接
        ja: TRACKER URL
        ko: TRACKER URL
        ru: TRACKER URL
        ms: TRACKER URL
        pt-br: TRACKER URL
      required: true
      type: text
