additionalProperties:
  formFields:
    - default: 40042
      edit: true
      envKey: PANEL_APP_PORT_HTTP
      labelEn: Port
      labelZh: 端口
      label:
        en: Port
        zh: 端口
        zh-Hant: 端口
        ja: Port
        ko: Port
        ru: Port
        ms: Port
        pt-br: Port
      required: true
      rule: paramPort
      type: number
    - default: "sk-xxx"
      edit: true
      envKey: API_KEY
      labelEn: OPENAI API KEY
      labelZh: OPENAI API KEY
      label:
        en: OPENAI API KEY
        zh: OPENAI API KEY
        zh-Hant: OPENAI API KEY
        ja: OPENAI API KEY
        ko: OPENAI API KEY
        ru: OPENAI API KEY
        ms: OPENAI API KEY
        pt-br: OPENAI API KEY
      required: true
      type: text
    - default: "chatgptnextweb"
      edit: true
      envKey: SECRET_KEY
      labelEn: Access rights key, optional (recommended)
      labelZh: 访问权限密钥，可选(强烈建议填写)
      label:
        en: Access rights key, optional (recommended)
        zh: 访问权限密钥，可选(强烈建议填写)
        zh-Hant: 访问权限密钥，可选(强烈建议填写)
        ja: Access rights key, optional (recommended)
        ko: Access rights key, optional (recommended)
        ru: Access rights key, optional (recommended)
        ms: Access rights key, optional (recommended)
        pt-br: Access rights key, optional (recommended)
      random: true
      required: false
      rule: paramComplexity
      type: password
    - default: ""
      edit: true
      envKey: PROXY
      labelEn: Proxy (example：http://127.0.0.1:7890 user password)
      labelZh: 代理地址(例子：http://127.0.0.1:7890 user password)
      label:
        en: Proxy (example：http://127.0.0.1:7890 user password)
        zh: 代理地址(例子：http://127.0.0.1:7890 user password)
        zh-Hant: 代理地址(例子：http://127.0.0.1:7890 user password)
        ja: Proxy (example：http://127.0.0.1:7890 user password)
        ko: Proxy (example：http://127.0.0.1:7890 user password)
        ru: Proxy (example：http://127.0.0.1:7890 user password)
        ms: Proxy (example：http://127.0.0.1:7890 user password)
        pt-br: Proxy (example：http://127.0.0.1:7890 user password)
      required: false
      type: text
    - default: "https://api.openai.com"
      edit: true
      envKey: API_BASE_URL
      labelEn: API interface address
      labelZh: API接口地址
      label:
        en: API interface address
        zh: API接口地址
        zh-Hant: API接口地址
        ja: API interface address
        ko: API interface address
        ru: API interface address
        ms: API interface address
        pt-br: API interface address
      required: true
      type: text
