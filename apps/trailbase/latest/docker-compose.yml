services:
  trail:
    image: "trailbase/trailbase:latest@sha256:041f2f3c3e7565cf547d06ca5df00fe9283946fb7829ce314ecdce19fad18095"
    container_name: ${CONTAINER_NAME}
    restart: unless-stopped
    ports:
      - "${PANEL_APP_PORT_HTTP}:4000"
    environment:
      - RUST_BACKTRACE=1
    volumes:
      - "${APP_DATA_DIR:-./data}:/app/traildepot"
    healthcheck:
      test:
        - CMD-SHELL
        - curl --fail http://localhost:4000/api/healthcheck || exit 1
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 15s
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    networks:
      - 1panel-network
    labels:
      createdBy: "Apps"

networks:
  1panel-network:
    external: true
