services:
  hermitstash:
    image: "ghcr.io/dotcoocoo/hermitstash:1.14.5@sha256:e11dd9fd892472e0f3342906b53bc0e051e12fcead96b2d28af473a9a78032b9"
    container_name: ${CONTAINER_NAME}
    restart: unless-stopped
    init: true
    user: "1000:1000"
    networks:
      - 1panel-network
    ports:
      - "${PANEL_APP_PORT_HTTP}:3000"
    environment:
      - NODE_ENV=production
      - HERMITSTASH_TMPDIR=/dev/shm
      - PORT=3000
      - LOCAL_AUTH=true
      - REGISTRATION_OPEN=false
      - PUBLIC_UPLOAD=false
      - PASSKEY_ENABLED=true
      - CA_KEY_SEALED=auto
      - TLS_KEY_SEALED=auto
    shm_size: 256m
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - "${APP_DATA_DIR}:/app/data"
      - "${APP_UPLOADS_DIR}:/app/uploads"
    labels:
      createdBy: "Apps"

networks:
  1panel-network:
    external: true
